1，Hibernate的对象状态：<理解>
	临时状态：
	持久化状态：
	托管状态：
2，Hibernate加载策略：只能用其中一个<掌握>
	立即加载：加载关联数据，执行了2个查询语句（1-1默认加载方式）
		lazy="false"
	延迟加载：不加载关联数据（n-1,1-n,m-n默认的加载方式）
		lazy="true"
	预先抓取：加载关联数据，执行一个SQL语句（左外关联查询）
		fetch="join"
		执行的是左外连接的sql语句
3，Hibernate外键维护：在"一"方集合中进行配置<掌握>
	inverse="true":
		外键维护反转：由"多"方进行外键维护
	inverse="false":
		外键维护：由"一"方进行外键维护

	
	
	
	
	
	
	
	